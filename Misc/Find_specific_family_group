source ~/Dropbox/Scripts/General_utils.tcl

cd ~/desktop/Geo_v_all/2.0/Final_family_groups

set group_files [glob *faa]

foreach file $group_files {
	set group_seqs [string trim [openfile $file]]

	if {[regexp -all "YP_002949268.1" $group_seqs] > 0} {
		puts $file
	}
}